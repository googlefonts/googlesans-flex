# glyphspackage2ufo

Pre-requisites: none

Steps:
0. Setup Python
1. Check out `it-ad-wip-v2.0` in `$GITHUB_WORKSPACE/octavio`
2. Check out `main` in `$GITHUB_WORKSPACE/main`
3. Create a copy of `main` in target dir
4. Run import script

Outputs:
* `directory`: where the new UFO sources reside
